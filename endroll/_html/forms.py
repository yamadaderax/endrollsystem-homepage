from django import forms


# class ReserveForm(forms.Form):
#
#     name = forms.CharField(label='お名前(ニックネーム可)',max_length=100)
#     email = forms.EmailField(label='メールアドレス')
#     num = forms.ChoiceField(label='取り置き人数', choice([i += 1 for i in range(20)],))
